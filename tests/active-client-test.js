'use strict';

const assert = require('assert');

const sandbox = require('sinon').createSandbox();

const { ActiveClient, ActiveClientError } = require('./..');

describe('ActiveClient', () => {

	// your tests here...

});
